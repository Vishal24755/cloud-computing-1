{
  "name": "serverless-image-platform",
  "version": "1.0.0",
  "description": "Serverless image processing platform",
  "main": "src/handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "serverless": "^3.0.0"
  }
}
